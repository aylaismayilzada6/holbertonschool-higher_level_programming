#!/usr/bin/python3
def print_reversed_list_integer(my_list=[]):
    def print_reversed_list_integer(my_list=[]):
    temp = my_list.copy()
    temp.reverse()
    for num in temp:
        print("{:d}".format(num))
